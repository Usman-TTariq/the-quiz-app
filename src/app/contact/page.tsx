
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Contact Page",
    description: "This is the contact page for website"
}


export default function contact() {
    return (
        <div>this is contact page.</div>
    );
}